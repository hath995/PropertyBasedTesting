
import {max_difference} from '../problemset/ep6/max_difference';
import {check, gen, property, Generator} from 'testcheck';


describe('max difference', () => {
    it('should return the max difference A[j] - A[i] i < j', () => {

    });
});
